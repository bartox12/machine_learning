# Lab 03 — Linear & logistic regression

Ordinary least squares implemented from scratch and compared with scikit-learn. Regression metrics (MSE, RMSE, MAE, R²) on California Housing, then logistic regression: the role of the sigmoid function and Titanic survival prediction with classification metrics.

**Datasets:** California Housing (scikit-learn), Titanic (`../data/titan.csv`)
