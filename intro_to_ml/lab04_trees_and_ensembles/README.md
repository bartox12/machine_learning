# Lab 04 — Decision trees & ensemble methods

Decision tree classifiers with visualisation, then ensemble methods: random forest, AdaBoost, gradient boosting and XGBoost — compared on accuracy and training time.

**Datasets:** Iris, synthetic classification data
