# Lab 07 — Neural networks in PyTorch

PyTorch fundamentals (tensors, autograd), building and training networks for regression and classification, comparison of activation functions (ReLU vs alternatives), Adam vs SGD optimisers, and a demonstration of overfitting.

**Datasets:** synthetic data, Breast Cancer Wisconsin, make_moons
