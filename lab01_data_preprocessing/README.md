# Lab 01 — Data preprocessing

Exploratory data analysis and preparing data for modelling: handling missing values (imputation), feature scaling (`StandardScaler`, `MinMaxScaler`), one-hot encoding of categorical features (pandas and scikit-learn) and train/test splitting.

**Datasets:** Iris, Titanic (`../data/titan.csv`), California Housing (`../data/housing.csv`)
