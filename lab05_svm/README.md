# Lab 05 — Support Vector Machines

A linear SVM trained from scratch with (sub)gradient descent on the hinge loss, then scikit-learn SVMs with different kernels. Model selection with stratified k-fold cross-validation and `GridSearchCV`.

**Datasets:** synthetic blobs and circles, Breast Cancer Wisconsin
