# Lab 02 — Clustering

k-means implemented from scratch with k-means++ initialisation, compared against scikit-learn's `KMeans`, `AgglomerativeClustering` and `DBSCAN`. Cluster quality evaluated with the silhouette score; applied to customer segmentation.

**Datasets:** synthetic blobs, Iris, Mall Customers (`../data/Mall_Customers.csv`)
